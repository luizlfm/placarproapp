{
  "name": "placarpro-rules-test",
  "version": "1.0.0",
  "private": true,
  "description": "Testes das Firestore Security Rules contra o emulador (fluxos sensíveis das 5 fases de segurança).",
  "type": "module",
  "scripts": {
    "pretest": "node -e \"require('fs').copyFileSync('../../placarpro/firestore.rules','./firestore.rules')\"",
    "test": "firebase emulators:exec --config emu-config.json --only firestore --project placarpro-rules-test \"node run-tests.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^13.35.1"
  }
}
